# --- !Ups

CREATE TABLE "USERS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"NAME" VARCHAR NOT NULL, "PASSWORD" VARCHAR NOT NULL);
CREATE TABLE "TASKS" ("ID" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"TEXT" VARCHAR NOT NULL, "USERID" BIGINT);

# --- !Downs

DROP TABLE "TASKS";
DROP TABLE "USETS";